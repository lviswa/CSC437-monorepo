import { css } from "lit";

const styles = css`
  * {
    margin: 0;
    box-sizing: border-box;
  }

  img {
    max-width: 100%;
    display: block;
  }

  ul,
  menu {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  h1, h2, h3, h4, h5, h6, p {
    margin: 0;
  }
`;

export default { styles };
